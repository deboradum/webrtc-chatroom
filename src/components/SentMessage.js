function SentMessage({ text }) {
    return (
        <div className="p-2 my-2 mr-9 max-w-lg w-fit break-words rounded-lg bg-emerald-500 place-self-end">{text}</div>
    )
};

export default SentMessage;